{
      "name": "auth-app",
        "version": "1.0.0",
          "description": "Authentication project with Supabase & Cloudflare",
            "scripts": {
                "dev": "tailwindcss -i ./public/style.css -o ./public/output.css --watch",
                    "build": "tailwindcss -i ./public/style.css -o ./public/output.css --minify"
                      },
                        "devDependencies": {
                            "tailwindcss": "^3.4.1"
                              }
                              }